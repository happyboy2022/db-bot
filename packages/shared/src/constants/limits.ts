/**
 * Maximum number of affected rows for UPDATE/DELETE operations
 */
export const MAX_AFFECTED_ROWS = 1000;

/**
 * Approval expiry time in hours
 */
export const APPROVAL_EXPIRY_HOURS = 24;

/**
 * Maximum number of result rows to return for SELECT
 */
export const MAX_RESULT_ROWS = 200;

/**
 * Default SQL execution timeout in milliseconds
 */
export const DEFAULT_TIMEOUT_MS = 30000;

/**
 * Maximum SQL execution timeout in milliseconds
 */
export const MAX_TIMEOUT_MS = 120000;

/**
 * Minimum SQL execution timeout in milliseconds
 */
export const MIN_TIMEOUT_MS = 1000;
